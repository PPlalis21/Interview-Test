services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2019-latest
    container_name: sqlserver
    environment:
      SA_PASSWORD: "@Passw0rd"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"

  frontend:
    image: interview-test.client
    container_name: frontend
    ports:
      - "4200:80"
    build:
      context: ./Interview-Test.Client
      dockerfile: Dockerfile
